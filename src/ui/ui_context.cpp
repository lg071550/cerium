// UiContext is header-only for now (see ui_context.h). This translation unit
// exists so the build layout matches the planned structure and has a place
// for future non-inline context logic (focus, text editing, tooltips).
#include "ui_context.h"
